apiVersion: v1
kind: Service
metadata:
  name: catalog-svc
  labels:
    app.kubernetes.io/name: catalog-svc
    app.kubernetes.io/instance: catalog-svc
    app.kubernetes.io/component: app
    app.kubernetes.io/owner: capclouds.com
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: catalog
    app.kubernetes.io/instance: catalog
    app.kubernetes.io/component: app
    app.kubernetes.io/owner: capclouds.com