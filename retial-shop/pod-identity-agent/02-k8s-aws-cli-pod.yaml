apiVersion: v1
kind: Pod
metadata:
  name: aws-cli
  labels:
    app.kubernetes.io/name: aws-cli
spec:
  serviceAccountName: aws-cli-sa
  containers:
  - name: aws-cli
    image: amazon/aws-cli
    command:
      - sleep
      - infinity
